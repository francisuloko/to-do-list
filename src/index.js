import _ from 'lodash'
import './style.css'

function component() {
    const Tasks = [
      {
        description: 'Task descriptions',
        completed: false,
        index: 0,
      }
    ];
  }
  
document.body.appendChild(component());
